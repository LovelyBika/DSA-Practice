#include <bits/stdc++.h>
using namespace std;
void fc(vector <string>s, int si)
{
    for(auto s1:s){
        for(auto s2:s1){
            if(s2==)
        }
    }
}

int main()
{
   vector <string>v;
   int n;
   cin>>n;
   string str;
   for(int i=0;i<n;i++){
    cin>>str;
    v.push_back(str);
   }
fc(v,v.size());
return 0;

}